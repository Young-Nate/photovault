// Map of blog post slugs to featured images
// Used as fallback when featuredImageUrl is not set in DB
export const blogImageMap: Record<string, string> = {
  // Add slug-to-image mappings here as needed
};
